{% extends '::template_back.html.twig' %}

{% block body %}
<div class="page-header">
  <h1>TOP 10 des posteurs <small>Une publication rapporte 1 point</small></h1>
</div>

<table class="table table-striped">
{% for row in array %}
	<tr>
		<td>#{{ loop.index }}</td>
		<td>{{ row['user'].name }}</td>
		<td>{{ row['user'].surname }}</td>
		<td>{% if(row['user'].team == 'polecom') %}Pôlecom{% else %}Baudimont{% endif %}</td>
		<td>{{ row['value'] }} points</td>
	</tr>
{% endfor %}
</table>

{% endblock %}