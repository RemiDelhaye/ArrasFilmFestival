{% extends '::template_back.html.twig' %}

{% block body %}

<div class="row">
	<div class="span8 offset2">
		<form class="form-horizontal" action="{{ path('photo_create') }}" method="post" {{ form_enctype(form) }}>
		    <fieldset>
		    	<legend>Ajouter des photos</legend>

		    	{{ form_errors(form) }}

		    	<div class="control-group">
			    	{{ form_label(form.image, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
			    	<div class="controls">
				    	{{ form_widget(form.image, { 'attr': { 'class': 'span4' } }) }}
				    </div>
				    {{ form_errors(form.image) }}
				</div>

		    	<div class="control-group">
			    	{{ form_label(form.title, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
			    	<div class="controls">
				    	{{ form_widget(form.title, { 'attr': { 'placeholder': 'Titre de la photo', 'class': 'span4' } }) }}
				    </div>
				    {{ form_errors(form.title) }}
				</div>

		    	<div class="control-group">
			    	{{ form_label(form.content, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
			    	<div class="controls">
				    	{{ form_widget(form.content, { 'attr': { 'placeholder': 'Description de la photo', 'class': 'span4' } }) }}
				    </div>
				    {{ form_errors(form.content) }}
				</div>

					{{ form_widget(form._token) }}

				<div class="form-actions">
				  	<input type="submit" class="btn btn-primary" />
			    	<input type="reset" class="btn" />
				</div>
			</fieldset>
		</form>
	</div>
</div>

{% endblock %}